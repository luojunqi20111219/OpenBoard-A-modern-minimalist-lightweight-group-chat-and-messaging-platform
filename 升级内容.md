# 🚀 信语 (OpenBoard) v6.0 版本升级内容

我们非常高兴地宣布，**信语 (OpenBoard)** 已正式升级至 **v6.0** 版本！

在此次版本迭代中，我们实现了从单一的网页端工具向**多端（Android 原生 + C++ 桌面端 + 现代 Web 网页端）全场景即时通信生态**的重大跨越。同时，我们在消息触达、安全架构、多设备会话管理及交互设计上进行了全方位的深度优化。

---

## 📱 1. 全新原生安卓端 (OpenBoardAndroid v6.0)
首个基于 **Kotlin + MVVM 架构** 研发的现代化原生 Android 客户端正式发布：
* **⚡ 双向实时 WebSocket**：基于协程异步通道实现低延迟、高并发的消息实时收发。
* **🔔 华为推送服务 (HMS Push)**：深度集成华为系统级推送 SDK，在 App 处于后台、进程被挂起或被系统深度休眠时，依然能实时唤醒并下发新消息提醒。
* **💬 消息长按多功能气泡菜单**：
  * **引用回复**：支持对特定消息进行引用并附带回复内容。
  * **平滑闪烁定位**：点击引用区域，消息列表会顺滑滚动并闪烁定位至被引用的原始消息位置。
  * **复制、撤回、删除**：全面支持文本长按划定复制，以及发送 2 分钟内的消息无缝撤回。
* **🖼️ 高清图片预览与保存**：集成了现代图片灯箱（Lightbox）预览组件，支持手势缩放及“保存至相册”功能。
* **📇 推荐好友名片**：在聊天窗口中一键点击推送好友名片，接收方气泡展示为精美卡片样式，点击名片头像可直接跳转并查看好友的详细资料与加好友入口。

---

## 💻 2. C++ 桌面客户端升级 (Windows WebView2 v6.0)
基于 C++ + 微软 Edge WebView2 内核编译的极致轻量级 Windows 原生客户端，迎来了高效率的交互调整：
* **⚙️ 系统开机自启**：
  * 在右下角托盘图标右键菜单中全新加入“开机自启”勾选项。
  * 点击即可一键安全修改/删除 Windows 注册表开机启动键（`HKEY_CURRENT_USER\...\Run`），带有自动勾选状态判定。
* **📥 任务栏普通最小化与关闭托盘挂机**：
  * 优化了窗口关闭与最小化逻辑。点击“最小化”按钮时，窗口正常收缩到任务栏（不会从任务栏消失），方便用户快速点击恢复；
  * 点击右上角“关闭 (X)”按钮时，程序将被隐藏至系统托盘，并继续在后台监听新消息推送。
* **🔔 系统通知与点击唤醒**：接收新消息时弹出标准的 Windows 系统级 Toast 通知横幅。双击托盘图标，或在收到消息时点击系统通知横幅，客户端会自动唤醒、显示并强制置顶激活。
* **✨ 任务栏与托盘图标闪烁**：有未读新消息且窗口不处于激活状态时，任务栏图标与系统托盘图标会同步交替闪烁，直至用户点击激活窗口。

---

## 🌐 3. 网页端 (Web Front v6.0) 交互升级
* **🪂 全局拖拽文件与图片上传**：
  * 引入了全局拖拽（Drag & Drop）检测机制。用户在聊天界面的任意位置拖入文件，即可唤起高颜值高模糊的“释放文件即可发送”磨砂遮罩。
  * 释放鼠标后，背景线程将立即自动上传文件并发送给当前会话，无需手动点击附件图标选择。
* **📇 名片分享与快速跳转**：
  * 网页端输入框底部工具栏新增“推荐名片”按钮。
  * 自动将好友信息格式化为 `[user_card:username:nickname]` 投递。对端接收后直接显示特制的“查看资料”按钮，点击可一键呼出好友管理界面并检索目标，支持直接发起会话。

---

## ⚙️ 4. 服务端 (Backend v6.0) 安全与稳定防护
* **🛡️ 多设备登录并发限制与“强制下线”**：
  * 新增 `user_devices` 关系表，严格绑定用户的 Session 与设备唯一 ID、推送 Token。
  * 限制同一个账号最多允许 2 台设备同时在线。当检测到第 3 台设备登录时，服务端会自动销毁最久远的一台设备的登录状态，并通过 WebSocket 或是 HMS 推送实时向其下发 `action: logout` 指令，强制客户端退回到登录页面。
* **🛡️ SQLite 连接用完强制释放**：
  * 重构了 FastAPI 依赖注入级别的 `get_db()` 数据库管理类。
  * 引入 `yield` 管理，确保每一次 HTTP 接口或 WebSocket 握手请求完结后，都会在物理层面强制关闭 SQLite 链接，彻底消除了 SQLite 在并发状态下的 `database is locked` 锁表报错。
* **🔐 JWT 安全与敏感路由过滤**：
  * 将原先基于简单随机 Hex 字符串的 Session 重构为标准的 **JWT 签名令牌**。
  * 对管理后台（`/admin`）等路径实施了后端路由级别的 Cookie JWT 鉴权强拦截，确保安全防护不会因前端禁用 JavaScript 而失效。
