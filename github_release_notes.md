# Release Notes (版本发布日志)

## 🏷️ 版本号：`v4.0.0`
## 📢 版本主题：首发原生安卓客户端与 HMS 推送生态集成

---

## 🚀 版本概述
这是 **信语 (OpenBoard)** 项目里程碑式的更新！在这之前的版本中，项目仅支持网页端（Web）。**在此版本中，我们正式推出了首个原生安卓客户端（OpenBoardAndroid）！**

本次更新的重中之重是**安卓客户端的全面引入**。与此同时，服务器端（Backend）进行了底层的安全与服务重构，**全力服务于安卓客户端的稳定运行**。服务器端新增了华为推送服务（HMS Push）、多设备在线安全校验及 API 文件白名单防护，为移动端提供了可靠、实时的后台推送与通信支撑。

---

## 📱 核心更新：原生安卓客户端 (OpenBoardAndroid) - 首次亮相！
我们使用 **Kotlin + MVVM** 架构为信语量身打造了原生的安卓客户端，带来极致流畅的移动端即时通信体验：

1. **🎨 极简现代 UI**：基于 Material Design 设计，界面美观流畅，支持平滑的消息滚动与微动画。
2. **⚡ WebSocket 实时收发**：支持文字、图片的低延迟即时收发，以及对方“正在输入中 (Typing...)”的状态实时反馈。
3. **🖼️ 图片大图预览与本地保存**：点击聊天中的图片即可打开高清大图预览层，并提供“保存到相册”功能，方便保存重要图片。
4. **📝 长按自由复制与气泡菜单**：优化长按交互，支持在气泡上方弹出气泡操作菜单（复制、回复、删除、撤回），并高亮显示文本，支持直接划定复制局部文字。
5. **🔗 引用回复与平滑滚动定位**：点击消息中引用的历史消息内容，列表会自动平滑滚动并闪烁定位到被引用的原始消息位置。
6. **🔔 华为推送服务系统级集成**：集成 HMS SDK，即便 App 在后台或被系统休眠，也能实时接收到新消息提醒。

---

## ⚙️ 后端升级：全力服务于安卓端的高效与安全
为了完美承载并服务于安卓客户端的推送与会话管理，后端（Server）进行了如下重大更新：

* **📲 华为推送服务 (HMS Push) 支持**：
  * 后端新增了 HMS OAuth 2.0 自动获取 token 机制与推送下发模块（`app/hms_push.py`）。
  * 在发送消息接口中引入了异步后台任务，当检测到安卓端不在线时，自动通过华为通道下发实时通知。
* **🔐 多设备安全并发限制与“强制下线”**：
  * 数据库新增 `user_devices` 表，绑定用户的每次登录 Session 与设备 ID、推送 Token。
  * 限制单账号最多允许 2 台设备同时在线。当检测到第 3 台设备登录时，服务器会自动将最老的设备 Session 清除，并发送 HMS 指令（`action: logout`）强制安卓客户端本地退回登录页面，确保账户安全。
* **🛡️ 文件上传白名单过滤**：
  * 为防止移动端上传漏洞，后端由黑名单升级为严格的 **白名单验证**，目前仅允许上传 `jpg`, `jpeg`, `png`, `gif`, `pdf`, `docx`, `txt`, `zip` 等安全格式文件。

---

## 🛠️ 编译与部署指南

### 安卓客户端 (Android)
1. 在 Android Studio 中导入 `OpenBoardAndroid` 项目。
2. 在 `app/src/main/java/com/openboard/nativeapp/data/api/RetrofitClient.kt` 中修改 `BASE_URL` 为您的服务器地址。
3. 连接您的安卓测试设备，点击 **Run** 即可。

### 服务端 (Server)
1. 确保已安装 Python 3.8+。
2. 运行 `run.bat` (Windows) 或 `run.sh` (Linux/macOS) 自动装包并一键启动。
